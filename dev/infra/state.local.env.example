# Shared R2 (S3-compatible) state-backend credentials — used by every infra
# module (cloudflare, gcp). Scoped to the pdx-tofu-state bucket.
# Copy to state.local.env (gitignored). Rotate in this one place.
AWS_ACCESS_KEY_ID=...
AWS_SECRET_ACCESS_KEY=...
