[package]
name = "schemas"
version = "0.1.0"
edition = "2021"
publish = false

[features]
inline = []

[dependencies]
flatbuffers = "24.3.25"
jomini = {version = "0.27" }

[dev-dependencies]
criterion = "0.5"
rand = "0.8"

[[bench]]
name = "schema_benchmark"
harness = false
