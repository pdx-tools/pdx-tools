{
  "private": true,
  "name": "worker",
  "version": "1.0.0",
  "description": "",
  "main": "worker/script.js",
  "author": "",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "esbuild index.ts --minify --bundle --outfile=worker/script.js",
    "test": "npm install --no-save typescript && tsc"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.1.3",
    "aws4fetch": "^1.0.13"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.0.0",
    "esbuild": "^0.13.8"
  }
}
